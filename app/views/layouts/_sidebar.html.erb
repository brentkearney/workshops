<div class="navbar-default sidebar" role="navigation">
  <div class="sidebar-nav navbar-collapse">
    <ul class="nav" id="side-menu">
      <li>
        <a href="/" id="home"><i class="fa fa-dashboard fa-fw"></i> Home</a>
      </li>
      <li>
        <%= link_to '<i class="fa fa-university fa-fw"></i> All Events'.html_safe, events_path, :id => 'all-events' %>
      </li>
      <li>
      <%= link_to '<i class="fa fa-suitcase fa-fw"></i> My Events'.html_safe, my_events_path, :id => 'my-events' %>
      </li>
      <li>
        <%= link_to '<i class="fa fa-forward fa-fw"></i> Future Events'.html_safe, events_future_path, :id => 'future-events' %>
      </li>
      <li>
        <%= link_to '<i class="fa fa-backward fa-fw"></i> Past Events'.html_safe, events_past_path, :id => 'past-events' %>
      </li>
      <% unless Global.location.all.empty? %>
      <li>
        <a href="#"><i class="fa fa-globe fa-fw"></i> Event Locations <span class="fa arrow"></span></a>
        <ul class="nav nav-second-level" id="event-location">
          <% Global.location.all.each do |location| %>
          <li>
            <%= link_to %Q(<i class="fa fa-location-arrow fa-fw"></i> #{location}).html_safe,
                        events_location_path(location), :id => "#{location}" %>
          </li>
          <% end %>
        </ul>
        <!-- /.nav-second-level -->
      </li>
      <% end %>
      <!-- /.unless Global.location.all.empty? -->

      <% if @event && @event.id %>
      <li class="spacer"></li>
      <li>
        <a href="#"><i class="fa fa-gears fa-fw"></i> <%= @event.code %> <span class="fa arrow"></span></a>
        <ul class="nav nav-second-level">
          <li>
            <%= link_to '<i class="fa fa-book fa-fw"></i> Details'.html_safe, event_path(@event), :id => 'event-details' %>
          </li>
          <li>
            <%= link_to '<i class="fa fa-calendar fa-fw"></i> Schedule'.html_safe, event_schedule_index_path(@event), :id => 'event-schedule' %>
          </li>
          <li>
            <%= link_to '<i class="fa fa-users fa-fw"></i> Members'.html_safe, event_memberships_path(@event), :id => 'event-memberships' %>
          </li>
        </ul>
        <!-- /.nav-second-level -->
      </li>
      <% end %>
      <!-- /.if @event -->
    </ul>
  </div>
  <!-- /.sidebar-collapse -->
</div>
<!-- /.navbar-static-side -->
